module.exports = function(grunt) {

  // Project configuration.
  grunt.initConfig({
    pkg: grunt.file.readJSON('package.json'),
    /*umd: {
      all: {
        src: 'src/TreeModel2.js',
        dest: 'dist/TreeModel2.js', // optional, if missing the src will be used
          objectToExport: 'Tree' // internal object that will be exported
          //amdModuleId: 'tree', // optional, if missing the AMD module will be anonymous
          //globalAlias: 'alias', // optional, changes the name of the global variable
      }
    },*/
    jshint: {
      jshintrc: '.jshintrc',
      all: ['Gruntfile.js', 'src/TreeModel.js', 'test/**/*.js'],
      reporter: 'jslint'
      //afterconcat: ['dist/TreeModel.js']
    }
  });

  //grunt.loadNpmTasks('grunt-umd');
  grunt.loadNpmTasks('grunt-contrib-jshint');

  // Default task(s).
  //grunt.registerTask('default');

};
